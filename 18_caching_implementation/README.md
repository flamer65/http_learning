# Lesson 18: Caching Implementation

Implement Express middleware for `Cache-Control`, `ETags`, and conditional requests!

## Quick Start

1. Install dependencies:
   ```bash
   bun install
   ```
2. Run the tests in watch mode:
   ```bash
   bun test --watch
   ```
3. Open `exercises/src/middleware/cache.ts` and follow the TODOs!
