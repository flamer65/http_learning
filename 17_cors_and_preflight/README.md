# Lesson 17: CORS Implementation

Implement CORS headers manually to understand how the browser and server negotiate access.

## Quick Start

1. Install dependencies:
   ```bash
   bun install
   ```
2. Run the tests in watch mode:
   ```bash
   bun test --watch
   ```
3. Open `exercises/src/middleware/cors.ts` and follow the TODOs!
